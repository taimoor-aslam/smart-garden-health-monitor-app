import 'package:flutter/material.dart';

class ColorSelection{
  final Color textColor;
  final Color iconColor;
  final Color backGroundColor;
  ColorSelection(this.textColor,this.iconColor,this.backGroundColor);
}