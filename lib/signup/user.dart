import 'package:flutter/material.dart';

class User {
  String name;
  String username;
  String email;
  String password;
  String confirmPassword;
  String role;

  // User(this.name, this.username, this.email, this.password,this.confirmPassword, this.role);
}
